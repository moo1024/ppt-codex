# ppt-master-v2 (Codex 판) — 작업 지침

AI PPT 자동생성 파이프라인. **아키텍처 v7 (확정)**: 사용자가 내용을 주면 Codex가 직접
내용 정리→컷 분할→디자인해서 `layout_spec.json`을 쓰고, python-pptx 네이티브 개체로
렌더한다 (텍스트 100% 편집 가능). 스팟/커버 이미지는 **Codex 내장 `$imagegen`**
(gpt-image-2 · ChatGPT 로그인 · API 키 불필요)로 뽑아 `assets/spots/`에 넣는다.
**v1~v6(GPT 통짜 이미지+분해)은 사용자가 반려하고 폐기했다 — 그 방향으로 돌아가지 말 것.**

## 표준 플로우 ("이 내용으로 PPT 만들어줘")

1. **내용 정리·컷 분할** — 핵심 메시지 단위로 페이지를 나눈다. **한 장 = 한 메시지.**
   구성 예: 표지(다크+글로우) → 목차 → 본문 N장 → 임팩트 디바이더(다크 한 문장) → 클로징.
2. **톤 분석 → 테마 선택** — **`docs/THEMES.md` §1을 열고** 발신 주체·수신자·목적을 진단해
   무게감을 정한다. 사용자가 톤을 명시하면 그것이 우선. 판단 결과는 시작 보고에 한 줄로 명시.
3. **스펙 작성** — **`docs/SPEC.md`를 열고** `projects/<slug>/pages/pNN/layout_spec.json` 작성.
   - **원문 증거 우선**: 입력 문서의 사진·지도·그래프·도표·스크린샷을 먼저 추출·목록화하고,
     발표 메시지를 직접 입증하는 장표에 원본 증거물로 배치한다. 새로 그린 도식만으로 원문
     증거를 대체하지 않는다. 출처·페이지를 작은 캡션으로 남기고, 저해상도·중복·장식용 이미지는 제외한다.
   - **문서 파싱 기본값**: HWP/HWPX/PDF/Office·스캔 문서는 `kordoc`을 우선 사용해 Markdown·표·이미지·OCR
     구조를 추출한다. 추출 결과와 페이지 렌더를 대조해 표·차트·사진 누락을 확인한다. `kordoc`을 쓸 수 없는
     환경에서만 `pdftotext`·`pdfimages`·페이지 렌더를 조합해 같은 점검을 수행한다.
4. **렌더 + 검수 루프**
   ```
   pptm rendernative <slug>  →  pptm export <slug> --force
   ① python3 tools/check_align.py <slug>   ← 0건이어야 함 (필수)
   ② projects/<slug>/output/thumbnails/slide-NN.jpg 썸네일 이미지를 직접 열어 눈으로 확인
   ```
   **게이트 0건 + 눈검수 통과 전엔 완성 선언 금지.** 어긋난 것 수정 → 재렌더.
5. **보고** — deck.pptx 경로 + 특이사항 + `explorer.exe`로 산출물 폴더 열기.

새 프로젝트: `pptm new <slug>` 또는 `projects/<slug>/pages/` 직접 생성.

**멀티에이전트 분업**: 8장+ 덱은 `.codex/agents/`로 분업 — `ppt-copy`(컷 분할·카피)
→ 메인이 톤·테마 확정 → `ppt-layout`(스펙+렌더) → `ppt-qa`(썸네일 전수 눈검수)
→ 메인이 수정 반영 후 재검수. **카피와 검수는 다른 에이전트가 맡아야** 자기 작업에
관대해지는 걸 막는다. 1~4장은 메인 단독.

## 이미지 생성 — Codex 내장 `$imagegen` (API 키 불필요)

커버·스팟 일러스트가 필요하면 **Codex 내장 이미지 도구**로 직접 생성한다.
별도 API 키가 필요 없고 **ChatGPT 로그인 사용량**으로 처리된다 (이미지는 일반 대비 3~5배 소진).

1. 자연어 또는 `$imagegen`으로 호출한다.
   예) `슬라이드 배경색 #F7F5F1에 정확히 맞춘 플랫 배경의 미니멀 라인 일러스트 생성 $imagegen`
2. **배경색을 슬라이드 배경과 반드시 일치**시킨다. 풀블리드/스크림은 글자 앉힐 어두운 면을 지정.
3. 생성된 PNG를 `projects/<slug>/assets/spots/<name>.png`로 저장(이동)한 뒤,
   layout_spec에서 image 아이템으로 참조한다 (상대경로는 프로젝트 폴더 기준):
   `{"type":"image","path":"assets/spots/<name>.png","x":0.55,"y":0.30,"w":0.38,"h":0.40}`
4. 빈 플레이스홀더 대신 실제 이미지를 넣는다. kie.ai·`pptm spot`은 이 판(Codex)에선 쓰지 않는다.
5. **기본 적용**: 이미지가 메시지 전달을 강화하는 덱이면 별도 지시가 없어도 표지·클로징·핵심 전환 장 중
   2~4곳에 이미지를 쓴다. 표지/클로징은 메시지와 여백이 맞는 Codex 생성 비주얼을 우선하며, 본문은 원문
   사진·그래프·지도 같은 증거물을 우선한다. 이미지마다 장표 배경·텍스트 앉힐 면·출처를 검수한다.

## 절대 규칙 (어기면 반려된다)

1. **여백은 호흡이지 공백이 아니다** — 최우선. 라이트 본문 페이지는 **콘텐츠 존
   y0.27~0.80을 의미 있는 내용으로 채운다.** 요소 사이 죽은 세로 공백(0.1 이상) 금지.
   얇은 도식(체인·바) 하나가 상단에 뜨고 아래가 비면 **즉시 반려감.**
   채우는 법(패딩 아님, 실질 내용): ① 근거·해설 문단 ② 지표 미니카드 행 ③ 항목별
   rationale 컬럼 ④ 보조 수치·주석 ⑤ 도식을 키우고 요소당 디테일 추가.
   예외는 진짜 미니멀(커버·디바이더·쇼케이스)뿐.
2. **제목·소제목은 반드시 Paperlogy Bold~ExtraBold.** 기본값은 `Paperlogy 8 ExtraBold`
   (헤드라인)·`Paperlogy 7 Bold`(소제목/카드·표 헤더)다. 원전이 씬(300/400)이어도 한글
   헤드라인은 굵게. 본문은 Pretendard를 유지하고, 얇은 웨이트는 보조 텍스트·라벨에만 쓴다.
   저장소의 `fonts/`는 협업자 설치용 공개 글꼴 패키지다. 일반 보고서는 이 기본값을 지키고,
   Gmarket Sans·The Jamsil은 친근한 브랜드/캠페인형 표지나 키 메시지에만 제한적으로 쓴다.
   새 폰트 파일을 저장소에 넣을 때는 재배포·번들 허용을 확인하고 `fonts/LICENSES/`에 전문을
   함께 추가한다. 상용·회사 전용·OS 기본 글꼴은 공개 배포 패키지에 넣지 않는다.
3. **TYPE SCALE 고정** — 헤드라인 23pt(다크 30) / 카드타이틀 13.5 / 본문 11.5 / 라벨 10 /
   캡션 8.5. 전 슬라이드 동일. 커버만 28~35 오버라이드 허용.
4. **문장형 헤드라인** — "~합니다." 완결 문장. 레이블식 제목("추진 배경") 금지.
5. **전달 우선** — 발표자가 말할 서사는 슬라이드에 안 쓴다. 불릿 줄당 ~40자, 페이지당
   3~4줄 상한. 수치는 텍스트가 아니라 도식으로. 결정적 메시지는 다크 디바이더 한 문장.
6. **채워진 바 안 텍스트는 `band`** — `card` + 별도 `text` 손좌표 금지 (세로 중앙이 안 맞음).
7. **실제 수치가 있는 그래프·표만 네이티브 `chart`/`table`로** — card 사각형 손좌표로
   가짜 막대 금지. 수신자가 수치를 직접 편집할 수 있어야 한다.
   **단 수치 없는 개념 비교(As-Is↔To-Be, 체크리스트)는 도형·카드로** — 억지 차트는 촌스럽다.
8. **단계 도식 표준 = 넘버 서클 체인** — 커넥터 라인 위에 넘버 chip, 단계명·캡션은 전부
   원 아래로. **체브론/화살표 도형 안에 글자를 넣는 스텝은 반려 확정 — 금지.**
9. **빈 플레이스홀더 금지** — "IMAGE" 같은 회색 자리표시 박스는 미완성으로 보인다.
   이미지가 없으면 프리미티브(card/shape/hairline/text)로 목업을 직접 그려 채울 것.
10. **테마 시그니처를 지켜라** — 색만 쓰지 말고 그 테마의 서피스 문법까지
    (anthropic=서피스 교대·코랄 1곳, cupertino=악센트 블루 1개·그림자 0,
    figma=컬러블록 화면당 1개, vercel=폴라리티 밴드).
11. **악센트는 절제** — 페이지당 1~2곳. 여백 최소 45%.
12. **폰트명은 Windows 레지스트리 등록명** — 한글 로컬라이즈명 폰트는 한글 이름으로.
    상세·판정법은 `docs/PITFALLS.md` §2.
13. **같은 패턴은 전수 수정** — 한 페이지를 지적받으면 grep으로 동일 패턴을 전 페이지
    쓸어 함께 고친다. 지적된 페이지만 고치면 재반려된다.

## 트랙 분기

| 요청 | 트랙 | 먼저 열 것 |
|---|---|---|
| 일반 PPT (16:9) | 표준 플로우 | `docs/THEMES.md` → `docs/SPEC.md` |
| **결과보고서(결보)** | A4 세로 문서형 | **`.codex/skills/gyeolbo/SKILL.md`를 읽고 그대로 발동** — 곧바로 만들지 말고 요구사항 브리프부터 받는다 |
| IT 화면설계서·스토리보드 | SB 트랙 | `docs/design-refs-260717.md` (d83 문법: 모노크롬+레드 주석 1색) |
| 프리미엄·시네마틱 | 풀블리드 | `docs/design-refs-paperlogy.md` (krlocal·kmong보다 우선) |

## 참조 지도 (필요할 때만 연다)

| 문서 | 언제 |
|---|---|
| `docs/SPEC.md` | **스펙 쓸 때** — 아이템 타입·레시피·조립 카탈로그·스팟 이미지 |
| `docs/THEMES.md` | **테마 고를 때** — 톤 분석 표, 테마 36종, 신규 테마 제작 |
| `docs/PITFALLS.md` | **렌더가 이상할 때** — 렌더러 함정, 폰트명 규칙, 썸네일 아티팩트 |
| `docs/design-refs-krlocal.md` §2 | 도식 29종 (심사장표·브로슈어·세일즈) |
| `docs/design-refs-kmong.md` §2~§4 | 도식 71종 + 강조 기법 + 안티패턴 |
| `docs/design-refs-paperlogy.md` | 프리미엄 문법 + 레시피 60종 (§7) |
| `docs/design-refs-gyeolbo-writing.md` | 결보 §0 절대 체크리스트 17항 |
| `docs/design-refs-uiux.md` | 상태색 표준·업종 팔레트 |
| `docs/design-refs-260717.md` | 스윔레인·간트·화면설계서 |

## 커맨드

```bash
source .venv/bin/activate
pptm rendernative <slug>          # layout_spec → deck.pptx
pptm export <slug> --force        # PDF + 썸네일 (검수용)
# 이미지 = Codex 내장 $imagegen (아래 '이미지 생성' 절 참조) — pptm spot·kie 안 씀
pptm status <slug>
python3 tools/check_align.py <slug>   # 정렬 게이트 (완성 전 필수)
python3 -m pytest tests/ -q           # 커밋 전 필수 (렌더러 수정 시 TDD)
```

- `projects/`는 gitignore (로컬 데이터). 코드/문서만 커밋.
- 산출물 보고 시 `explorer.exe`로 폴더를 열어주고 `\\wsl.localhost` 경로를 병기한다.
- 동시 세션이면 바탕화면 `결보ai-N` + repo `projects/sN-*`로 격리 — 남의 폴더 건드리지 말 것.
- **사용자 피드백으로 규칙이 바뀌면 이 파일(또는 해당 docs)에 즉시 반영할 것.**
